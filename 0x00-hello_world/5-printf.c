#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0, if successful
 */
int main(void)
{
	char string[] = "with proper grammar, but the outcome is a piece of art,";
	printf("%s\n", string);
}
